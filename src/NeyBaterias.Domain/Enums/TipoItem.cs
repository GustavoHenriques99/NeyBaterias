namespace NeyBaterias.Domain.Enums;

/// <summary>
/// Corresponde ao campo "tipo" da tabela item ('P' = Produto, 'S' = Serviço).
/// </summary>
public enum TipoItem
{
    Produto, // P = Produto.
    Servico // S = Servico.
}
